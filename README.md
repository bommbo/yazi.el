# yazi.el
Yazi file manager integration for Emacs using term.el

# Installation

```
(add-to-list 'load-path "/path/to/yazi.el")
(require 'yazi)
;; if you use meow
;;(add-hook 'term-mode-hook
;;          (lambda ()
;;            (meow-mode -1)
;;            (setq-local meow-normal-mode nil)))
```

# Function

yazi
yazi-cd
